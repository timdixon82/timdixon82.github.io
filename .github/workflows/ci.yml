# Continuous integration.
#
# Action versions were verified against GitHub and pinned to commit SHAs
# on 2026-05-21. The version tag is shown in the trailing comment. To
# update an action, change the SHA and the comment together.
#
# This is a stack-aware scaffold. Uncomment and adapt the block for the
# project's stack: a static front-end, PHP with MariaDB, or WordPress.

name: CI
on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      # --- Static front-end: HTML, CSS, JavaScript ---
      # - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0
      #   with:
      #     node-version: lts/*
      # - run: npm ci
      # - run: npm run lint
      # - run: npm test
      # - run: npm run build

      # --- PHP with MariaDB ---
      # - uses: shivammathur/setup-php@7c071dfe9dc99bdf297fa79cb49ea005b9fcadbc  # v2.37.1
      #   with:
      #     php-version: '8.3'
      # - run: composer install --no-interaction --no-progress
      # - run: vendor/bin/phpstan analyse
      # - run: vendor/bin/phpunit

      # --- WordPress ---
      # Test themes and plugins with the WordPress test suite. Adapt as needed.

      - name: Placeholder
        run: echo "Adapt this workflow to the project stack. See the comments above."
